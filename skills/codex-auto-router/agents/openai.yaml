interface:
  display_name: "Codex Auto Router"
  short_description: "Explicitly route bounded work to Terra"
  default_prompt: "Use $codex-auto-router to decide whether this task should stay with Root or run through a Terra worker."

policy:
  allow_implicit_invocation: false
